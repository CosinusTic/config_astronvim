# Config for C, Rust, Python, Assembly

## Setup
Setup lazy nvim, launch `nvim` and run `:Lazy`, then run the updates.

## Assembly
To make the assembly work, make sure you have `cargo` installed and run `cargo install asm_lsp`, then make sure it is in the path with `which asm-lsp`.
